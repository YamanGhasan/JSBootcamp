
// import * from "./cal.mjs";  

export default function squareArea(radius) {
    /* 💡 We don't care about the implementation here */
  }
  
  function squareCircumference(radius) {
    /* 💡 We don't care about the implementation here */
  }